if(ENABLE_PROBES_UNIX)
	add_oscap_test("capability.sh" LABELS unix)
	add_oscap_test("command_line.sh" LABELS unix)
	add_oscap_test("command_line_extra.sh" LABELS unix)
	add_oscap_test("dev_to_tty.sh" LABELS unix)
	add_oscap_test("empty_proc.sh" LABELS unix)
	add_oscap_test("loginuid.sh" LABELS unix)
	add_oscap_test("selinux_domain_label.sh" LABELS unix)
	add_oscap_test("sessionid.sh" LABELS unix)
	add_oscap_test("test_probes_process58_offline_mode.sh" LABELS unix)
endif()
