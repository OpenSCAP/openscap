EXTRA_DIST = $(top_srcdir)/tests/test_common.sh

TESTS_ENVIRONMENT= \
	builddir=$(top_builddir) \
	$(top_builddir)/run

DISTCLEANFILES = *.log *.out*
CLEANFILES = *.log *.out*

TESTS = all.sh

EXTRA_DIST += \
	all.sh
