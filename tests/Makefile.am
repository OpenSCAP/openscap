if WANT_PROBES
PROBE_SUBDIRS = probes mitre
endif

if WANT_SCE
SCE_SUBDIRS = sce
endif

if WANT_BINDINGS
BINDINGS_SUBDIRS = bindings
endif

SUBDIRS = API DS $(PROBE_SUBDIRS) $(SCE_SUBDIRS) $(BINDINGS_SUBDIRS)
