This folder cotains SELinux policy for oscap tool and openscap probes.

INSTALL
-------

$ ./configure --enable-selinux_policy

$ make

# make install

# semodule -i /usr/share/selinux/packages/oscap.pp

# restorecon -Rv /usr/bin/oscap /usr/libexec/openscap
